// Convert a no into string without using string method and return it's typeof.

const noIntoString = (num) => {
    let str = num + '';
    // console.log(num, str);
    return typeof(str);
}
console.log(noIntoString(75));



// ---------------------------------------------------------------------------------------------------------
